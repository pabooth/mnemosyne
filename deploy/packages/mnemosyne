#!/bin/sh
set -eu

if [ "${1:-}" = "--version" ]; then
  printf 'mnemosyne %s\n' "$(cat /usr/share/mnemosyne/VERSION)"
  exit 0
fi

exec docker compose \
  --env-file /etc/mnemosyne/mnemosyne.env \
  -f /usr/share/mnemosyne/docker-compose.yml \
  "$@"
